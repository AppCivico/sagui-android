<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="8dp"
    android:layout_marginLeft="16dp"
    android:layout_marginRight="16dp"
    android:background="@drawable/bg_gray_r8"
    android:clickable="true"
    android:foreground="?attr/selectableItemBackground"
    android:padding="8dp">

    <android.support.v7.widget.AppCompatImageView
        android:id="@+id/ivIcon"
        android:layout_width="38dp"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginEnd="8dp"
        android:layout_marginRight="8dp"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        app:srcCompat="@drawable/ic_pending"
        app:tint="@color/colorAccent"/>

    <com.eokoe.sagui.widgets.CustomTextView
        android:id="@+id/tvEvent"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/ivIcon"
        android:layout_toRightOf="@id/ivIcon"
        android:textColor="@color/colorTextSecondary"
        tools:text="Enquete ainda não enviada"/>

    <com.eokoe.sagui.widgets.CustomTextView
        android:id="@+id/tvMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/tvEvent"
        android:layout_toEndOf="@id/ivIcon"
        android:layout_toRightOf="@id/ivIcon"
        android:textColor="@color/colorTextPrimary"
        tools:text="Você respondeu a enquete sobre Saúde que não foi enviada por falta de conexão."/>
</RelativeLayout>